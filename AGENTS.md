# AGENTS Instructions

- Never commit `.env` or secrets.
- Use local repository artifacts as the source of truth for PDLC.
- Keep PDLC status and links in `_bmad-output/status/` and related local docs.
- When changing workflows/agents, update docs under `/docs`.
